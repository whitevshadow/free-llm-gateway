# Pydantic request/response schemas.
#
# The native-UI schemas (chat, analytics, user) were removed in the single-model
# consolidation; the /v1 compatibility endpoints define their own request models
# inline. New gateway-scoped schemas can be added here.
